import type { Cabin } from "./Cabin";

/**
 * CabinDetails - Full cabin representation for detail views
 * 
 * Currently identical to Cabin, but kept separate for future extensibility.
 * Future: Can add computed fields, related entities, etc.
 */
export type CabinDetails = Cabin;

