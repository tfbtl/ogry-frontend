/**
 * CabinId - Unique identifier for a cabin
 * 
 * Currently supports both string and number for backward compatibility.
 * Future: Consider branded type for stronger type safety.
 */
export type CabinId = string | number;

